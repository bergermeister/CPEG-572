#include <iostream>
#include <string>

int main( int aiArgC, char** acpArgV )
{
   int kiStatus = 0;

   std::string koLine = "";

   while( koLine.compare( "quit" ) != 0 )
   {
      std::getline( std::cin, koLine );
      koLine.find( )
      if( koLine.compare( "Hammer" ) )
      {

      }
      else if( koLine.compare( "De-hammer" ) )
      {

      }
      else if( koLine.compare( "Burst Error Corrector" ) )
      {

      }
      else if( koLine.compare( "quit" ) )
      {

      }
      else
      {
         std::cout << "Usage: " << std::endl;
         std::cout << "   Hammer    
      }
   }

   return( kiStatus );
}
